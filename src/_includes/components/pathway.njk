{# src/_includes/components/pathway.njk #}
{% macro pathway(path) %}
<div class="flex items-center gap-2 text-sm">
  {% for node in path %}
    {% if not loop.first %}
      <span aria-hidden="true" class="opacity-60">→</span>
    {% endif %}
    <a
      href="{{ node.url }}"
      class="link link-hover underline-offset-2 decoration-dotted rounded-sm px-0.5 focus:outline-none focus-visible:ring focus-visible:ring-primary/40"
    >
      {{ node.title }}
    </a>
  {% endfor %}
</div>
{% endmacro %}