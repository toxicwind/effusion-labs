// generated by finalize-structure.mjs (fixed)
export default {
  eleventyComputed: {
    // Respect explicit permalinks; otherwise normalize to /concepts/*
    permalink: (data) => {
      if (data.permalink) return data.permalink;
      // Directory index → /concepts/
      if (data.page.fileSlug === 'index' || data.page.fileSlug === '_index') {
        return '/concepts/';
      }
      // Default detail pages
      return `/concepts/${data.page.fileSlug}/`;
    },
    layout: (data) => data.layout ?? 'layouts/base.njk',
  },
};
