---
permalink: "/resume/"
title: "Resume"
layout: false
eleventyExcludeFromCollections: true
---

{# src/resume/index.njk — rebuilt + ADAPTIVE based on array sizes; no app.css #}
<!DOCTYPE html>
<html lang="en" class="scroll-pt-16">
<head>
  <!-- Meta -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="color-scheme" content="light dark">
  <meta name="theme-color" media="(prefers-color-scheme: light)" content="#ffffff">
  <meta name="theme-color" media="(prefers-color-scheme: dark)" content="#0b0b0e">
  <meta name="msapplication-TileColor" content="#0b0b0e">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

  {% set siteUrl = (site and site.url) or "" %}
  {% from "macros/meta.njk" import buildPageTitle, buildPageDesc %}
  {% set pageTitle = buildPageTitle(resume, title) %}
  {% set pageDesc  = buildPageDesc(resume) %}
  {% set canonicalPath = "/resume/" | url %}

  {# prefer absolute if site.url exists #}
  {% if site and site.url %}
    {% set canonicalHref = site.url + canonicalPath %}
  {% else %}
    {% set canonicalHref = canonicalPath %}
  {% endif %}

  {# also precompute Home link for Breadcrumb JSON-LD #}
  {% set homePath = "/" | url %}
  {% if site and site.url %}
    {% set homeHref = site.url + homePath %}
  {% else %}
    {% set homeHref = homePath %}
  {% endif %}

  <title>{{ pageTitle }}</title>
  <link rel="canonical" href="{{ canonicalHref }}">
  <meta name="description" content="{{ pageDesc }}">

  <!-- Open Graph -->
  <meta property="og:type" content="profile">
  <meta property="og:title" content="{{ pageTitle }}">
  <meta property="og:description" content="{{ pageDesc }}">
  <meta property="og:url" content="{{ canonicalHref }}">
  {% if resume and resume.ogImage %}
  <meta property="og:image" content="{{ resume.ogImage | url }}">
  {% endif %}

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ pageTitle }}">
  <meta name="twitter:description" content="{{ pageDesc }}">
  {% if resume and resume.ogImage %}
  <meta name="twitter:image" content="{{ resume.ogImage | url }}">
  {% endif %}

  <!-- DaisyUI v5 + Tailwind v4 (CDN) -->
  <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=Rubik:wght@400..900&family=Space+Grotesk:wght@400..700&display=swap" rel="stylesheet">

  <!-- Tiny CSS: skip-link + print polish only -->
  <style>
    .skip-link{position:absolute;left:-9999px;top:auto;width:1px;height:1px;overflow:hidden}
    .skip-link:focus{left:1rem;top:1rem;width:auto;height:auto;padding:.5rem .75rem;z-index:1000}
    @media print{
      @page{margin:.6in}
      .no-print,.navbar{display:none!important}
      .shadow,.shadow-lg,.shadow-xl{box-shadow:none!important}
      body{background:#fff!important;color:#000!important}
      a{color:#000!important;text-decoration:none!important}
      .card{background:#fff!important;border-color:#ddd!important}
    }
  </style>
</head>

<body class="bg-base-100 text-base-content font-body antialiased selection:bg-primary/20" data-theme="nord">
  <a href="#main" class="skip-link btn btn-sm">Skip to main content</a>

  <!-- Top bar -->
  <div class="navbar bg-base-100/80 backdrop-blur border-b border-base-200 sticky top-0 z-40">
    <div class="flex-1">
      <a href="{{ '/' | url }}" class="btn btn-ghost text-base font-semibold">
        {{ resume and resume.name or 'Resume' }}
      </a>
    </div>
    <div class="flex-none gap-2">
      <div class="join no-print">
        <input class="join-item btn btn-sm btn-outline theme-controller" type="radio" name="theme-top" aria-label="Nord" value="nord">
        <input class="join-item btn btn-sm btn-outline theme-controller" type="radio" name="theme-top" aria-label="Sunset" value="sunset">
      </div>
      <button class="btn btn-sm btn-outline no-print" onclick="window.print()" title="Print / Save PDF">Print</button>
    </div>
  </div>

  <!-- Main grid -->
  <main id="main" class="container mx-auto px-4 sm:px-6 lg:px-8 my-8">
    <div class="grid grid-cols-12 gap-6">
      <!-- LEFT RAIL (sticky on lg+) -->
      <aside class="col-span-12 lg:col-span-4">
        <div class="space-y-6 lg:sticky lg:top-6">
          <!-- Identity -->
          <section class="card shadow-sm border border-base-300/40 rounded-box">
            <div class="card-body">
              <h1 class="text-4xl md:text-5xl font-black tracking-tight">
                {{ resume and resume.name or "Your Name" }}
              </h1>
              <p class="mt-2 text-lg opacity-80">
                {{ resume and resume.title or "Creative Technologist & Engineer" }}
              </p>

              <div class="mt-4 flex flex-wrap gap-2 text-sm items-center">
                {% if resume and resume.location %}
                  <span class="badge badge-outline rounded-full">{{ resume.location }}</span>
                {% endif %}
                {% if resume and resume.phoneNote %}
                  <span class="tooltip tooltip-bottom" data-tip="{{ resume.phoneNote }}">
                    <span class="badge badge-outline rounded-full">Phone on request</span>
                  </span>
                {% endif %}
                {% if resume and resume.email %}
                  <a class="badge badge-outline rounded-full" href="mailto:{{ resume.email }}" onclick="copyToClipboard('{{ resume.email | escape }}'); return false;" title="Copy email">{{ resume.email }}</a>
                {% endif %}
                {% if resume and resume.linkedin %}
                  <a class="badge badge-outline rounded-full" href="{{ resume.linkedin }}" target="_blank" rel="noopener">LinkedIn</a>
                {% endif %}
                {% if resume and resume.website %}
                  <a class="badge badge-outline rounded-full" href="{{ resume.website }}" target="_blank" rel="noopener">Website</a>
                {% endif %}
                {% if resume and resume.github %}
                  <a class="badge badge-outline rounded-full" href="{{ resume.github }}" target="_blank" rel="noopener">GitHub</a>
                {% endif %}
              </div>

              {% if resume and resume.downloadPdf %}
              <div class="mt-6">
                <a href="{{ resume.downloadPdf | url }}" class="btn btn-outline btn-sm rounded-full no-print">Download PDF</a>
              </div>
              {% endif %}
            </div>
          </section>

          {% if resume and resume.summary %}
          <section class="card shadow-sm border border-base-300/40 rounded-box">
            <div class="card-body prose prose-sm sm:prose-base dark:prose-invert max-w-none">
              <h2 class="m-0">Summary</h2>
              <p class="mt-2">{{ resume.summary }}</p>
            </div>
          </section>
          {% endif %}

          {# =========================
             HIGHLIGHTS — adaptive UI
             ========================= #}
          {% set _hc = 0 %}
          {% if resume and resume.highlights %}{% set _hc = resume.highlights | length %}{% endif %}
          {% if _hc %}
          <section class="card shadow-sm border border-base-300/40 rounded-box">
            <div class="card-body">
              <h2 class="text-[11px] uppercase tracking-widest opacity-60">Highlights</h2>

              {# few (≤3): stats row; mid (4–8): compact KPI cards; many (≥9): dense compact cards #}
              {% if _hc <= 3 %}
                <div class="stats stats-vertical md:stats-horizontal w-full mt-2">
                  {% for s in resume.highlights %}
                  <div class="stat px-4">
                    <div class="stat-title">{{ s.title }}</div>
                    <div class="stat-value text-3xl">{{ s.value }}</div>
                    {% if s.sub %}<div class="stat-desc">{{ s.sub }}</div>{% endif %}
                  </div>
                  {% endfor %}
                </div>

              {% elif _hc <= 8 %}
                <div class="mt-3 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-3">
                  {% for s in resume.highlights %}
                  <div class="card card-compact border border-base-300/40 rounded-box">
                    <div class="card-body py-3">
                      <div class="text-[11px] uppercase tracking-widest opacity-60">{{ s.title }}</div>
                      <div class="text-2xl font-semibold leading-tight">{{ s.value }}</div>
                      {% if s.sub %}<div class="text-xs opacity-70 truncate">{{ s.sub }}</div>{% endif %}
                    </div>
                  </div>
                  {% endfor %}
                </div>

              {% else %}
                <div class="mt-3 grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 xl:grid-cols-6 gap-2">
                  {% for s in resume.highlights %}
                  <div class="card card-compact border border-base-300/40 rounded-box">
                    <div class="card-body py-2">
                      <div class="text-[10px] uppercase tracking-widest opacity-60 truncate">{{ s.title }}</div>
                      <div class="text-lg font-semibold leading-tight">{{ s.value }}</div>
                      {% if s.sub %}<div class="text-[11px] opacity-70 truncate">{{ s.sub }}</div>{% endif %}
                    </div>
                  </div>
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </section>
          {% endif %}

          {% if resume and (resume.skills or resume.tools) %}
          <section class="card shadow-sm border border-base-300/40 rounded-box">
            <div class="card-body">
              <h2 class="text-[11px] uppercase tracking-widest opacity-60">Skills &amp; Tools</h2>

              {% if resume.skills and resume.skills.groups %}
                {% for g in resume.skills.groups %}
                  {% set _count = g.items | length %}
                  {% set _badgeSize = 'badge-sm' %}{% if _count > 12 %}{% set _badgeSize = 'badge-xs' %}{% endif %}
                  <h3 class="mt-3 text-sm font-semibold">{{ g.name }}</h3>
                  <div class="mt-2 flex flex-wrap gap-2">
                    {% for item in g.items %}
                      {% if item.hint %}
                        <span class="badge badge-ghost rounded-full tooltip {{ _badgeSize }}" data-tip="{{ item.hint }}">{{ item.name }}</span>
                      {% else %}
                        <span class="badge badge-ghost rounded-full {{ _badgeSize }}">{{ item.name }}</span>
                      {% endif %}
                    {% endfor %}
                  </div>
                {% endfor %}
              {% elif resume.skills %}
                {% set _flatCount = resume.skills | length %}
                {% set _flatSize = 'badge-sm' %}{% if _flatCount > 16 %}{% set _flatSize = 'badge-xs' %}{% endif %}
                <div class="mt-2 flex flex-wrap gap-2">
                  {% for s in resume.skills %}<span class="badge badge-ghost rounded-full {{ _flatSize }}">{{ s }}</span>{% endfor %}
                </div>
              {% endif %}

              {% if resume.tools %}
                {% set _toolCount = resume.tools | length %}
                {% set _toolSize = 'badge-sm' %}{% if _toolCount > 14 %}{% set _toolSize = 'badge-xs' %}{% endif %}
                <h3 class="mt-4 text-sm font-semibold">Tools</h3>
                <div class="mt-2 flex flex-wrap gap-2">
                  {% for t in resume.tools %}<span class="badge badge-ghost rounded-full {{ _toolSize }}">{{ t }}</span>{% endfor %}
                </div>
              {% endif %}
            </div>
          </section>
          {% endif %}
        </div>
      </aside>

      <!-- RIGHT COLUMN -->
      <section class="col-span-12 lg:col-span-8 space-y-6">
        {% if resume and resume.experience %}
        <section class="card shadow-sm border border-base-300/40 rounded-box">
          <div class="card-body">
            <h2 class="text-[11px] uppercase tracking-widest opacity-60">Experience</h2>
            <div class="mt-3 grid gap-4">
              {% for job in resume.experience %}
              <article class="card bg-base-100 shadow-sm border border-base-300/40 rounded-box">
                <div class="card-body">
                  <div class="flex flex-wrap items-baseline justify-between gap-2">
                    <h3 class="text-lg font-semibold tracking-tight">
                      {{ job.company }}{% if job.role %} <span class="font-normal">— {{ job.role }}</span>{% endif %}
                    </h3>
                    <div class="text-xs md:text-sm opacity-70 text-right">
                      <span>{{ job.location }}</span>
                      <span class="mx-2 hidden md:inline">•</span>
                      <span class="block md:inline">{{ job.start }}{% if job.end %} — {{ job.end }}{% else %} — Now{% endif %}</span>
                    </div>
                  </div>

                  {% if job.highlights %}
                  {% set _maxBul = 5 %}
                  <ul class="mt-2 space-y-1.5 text-[15px] leading-6">
                    {% for h in job.highlights %}
                      {% if loop.index0 < _maxBul %}
                      <li class="pl-4 relative">
                        <span class="absolute left-0 top-2 w-1.5 h-1.5 rounded-full bg-base-content/30"></span>
                        {{ h }}
                      </li>
                      {% endif %}
                    {% endfor %}
                  </ul>
                  {% if job.highlights | length > _maxBul %}
                  <details class="mt-2">
                    <summary class="link text-sm opacity-80">Show {{ job.highlights | length - _maxBul }} more</summary>
                    <ul class="mt-2 space-y-1.5 text-[15px] leading-6">
                      {% for h in job.highlights %}
                        {% if loop.index0 >= _maxBul %}
                        <li class="pl-4 relative">
                          <span class="absolute left-0 top-2 w-1.5 h-1.5 rounded-full bg-base-content/20"></span>
                          {{ h }}
                        </li>
                        {% endif %}
                      {% endfor %}
                    </ul>
                  </details>
                  {% endif %}
                  {% endif %}

                  {% if job.tags %}
                  {% set _jt = job.tags | length %}
                  {% set _jtSize = 'badge-sm' %}{% if _jt > 10 %}{% set _jtSize = 'badge-xs' %}{% endif %}
                  <div class="mt-3 flex flex-wrap gap-2">
                    {% for tag in job.tags %}<span class="badge badge-ghost rounded-full {{ _jtSize }}">{{ tag }}</span>{% endfor %}
                  </div>
                  {% endif %}
                </div>
              </article>
              {% endfor %}
            </div>
          </div>
        </section>
        {% endif %}

        {% if resume and resume.projects %}
        {% set _pc = resume.projects | length %}
        {% set _projGrid = 'grid grid-cols-1 sm:grid-cols-2 gap-4' %}
        {% if _pc <= 1 %}
          {% set _projGrid = 'grid grid-cols-1 gap-4' %}
        {% elif _pc <= 6 %}
          {% set _projGrid = 'grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4' %}
        {% else %}
          {% set _projGrid = 'grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4' %}
        {% endif %}
        <section class="card shadow-sm border border-base-300/40 rounded-box">
          <div class="card-body">
            <h2 class="text-[11px] uppercase tracking-widest opacity-60">Selected Projects</h2>
            <div class="mt-3 {{ _projGrid }}">
              {% for p in resume.projects %}
              <article class="card card-sm bg-base-100 shadow-sm border border-base-300/40 rounded-box hover:shadow-md transition-shadow">
                <div class="card-body">
                  <div class="flex items-center gap-2">
                    <h3 class="card-title text-sm">
                      {% if p.url %}<a href="{{ p.url }}" target="_blank" rel="noopener">{{ p.name }}</a>{% else %}{{ p.name }}{% endif %}
                    </h3>
                    {% if p.badge %}<span class="badge badge-neutral badge-sm rounded-full">{{ p.badge }}</span>{% endif %}
                  </div>
                  {% if p.subtitle %}<p class="mt-1 text-xs opacity-70">{{ p.subtitle }}</p>{% endif %}
                  {% if p.blurb %}<p class="mt-2 text-sm leading-6">{{ p.blurb }}</p>{% endif %}
                  {% if p.tags %}
                  {% set _pt = p.tags | length %}
                  {% set _ptSize = 'badge-sm' %}{% if _pt > 8 %}{% set _ptSize = 'badge-xs' %}{% endif %}
                  <div class="mt-3 flex flex-wrap gap-2">
                    {% for t in p.tags %}<span class="badge badge-ghost rounded-full {{ _ptSize }}">{{ t }}</span>{% endfor %}
                  </div>
                  {% endif %}
                </div>
              </article>
              {% endfor %}
            </div>
          </div>
        </section>
        {% endif %}

        {% if resume and resume.education %}
        <section class="card shadow-sm border border-base-300/40 rounded-box">
          <div class="card-body">
            <h2 class="text-[11px] uppercase tracking-widest opacity-60">Education</h2>
            <div class="mt-3 grid grid-cols-1 md:grid-cols-2 gap-4">
              {% for e in resume.education %}
              <div class="card card-sm shadow-sm border border-base-300/40 rounded-box">
                <div class="card-body">
                  <div class="flex items-baseline justify-between gap-3 flex-wrap">
                    <h3 class="text-base font-medium m-0">{{ e.school }}</h3>
                    <div class="text-sm opacity-80">{{ e.start }}{% if e.end %} — {{ e.end }}{% endif %}</div>
                  </div>
                  <div class="text-sm opacity-90 mt-1">
                    {{ e.degree }}{% if e.meta %} — {{ e.meta }}{% endif %}
                  </div>
                </div>
              </div>
              {% endfor %}
            </div>
          </div>
        </section>
        {% endif %}

        <!-- Footer -->
        <footer class="flex items-center justify-between pt-2 pb-10 no-print">
          <small class="opacity-60">
            Last updated: {{ resume.updated | date('yyyy-MM-dd') }}
          </small>
          <div class="join">
            <input class="join-item btn btn-sm btn-outline theme-controller" type="radio" name="theme-foot" aria-label="Nord" value="nord">
            <input class="join-item btn btn-sm btn-outline theme-controller" type="radio" name="theme-foot" aria-label="Sunset" value="sunset">
          </div>
        </footer>
      </section>
    </div>
  </main>

  <!-- Toaster -->
  <div id="toaster" class="toast toast-end z-[100]"></div>

  <!-- JSON-LD (absolute URLs) -->
  {% set sameAsLinks = [resume.website, resume.linkedin, resume.github] | compactUnique %}
  <script type="application/ld+json">
  {
    "@context":"https://schema.org",
    "@type":"Person",
    "name": {{ (resume and resume.name) | dump | safe }},
    "jobTitle": {{ (resume and resume.title) | dump | safe }},
    {% if resume and resume.ogImage %}"image": {{ resume.ogImage | url | dump | safe }},{% endif %}
    {% if resume and resume.address %}
    "address": {{ resume.address | dump | safe }},
    {% elseif resume and resume.location %}
    "address": { "@type":"PostalAddress", "addressLocality": {{ resume.location | dump | safe }} },
    {% endif %}
    {% if resume and resume.email %}"email":"mailto:{{ resume.email }}",{% endif %}
    {% if sameAsLinks | length %}"sameAs": {{ sameAsLinks | dump | safe }},{% endif %}
    "url": {{ canonicalHref | dump | safe }}
  }
  </script>
  <script type="application/ld+json">
  {
    "@context":"https://schema.org",
    "@type":"BreadcrumbList",
    "itemListElement": [
      { "@type":"ListItem","position":1,"name":"Home","item": {{ homeHref | dump | safe }} },
      { "@type":"ListItem","position":2,"name":"Resume","item": {{ canonicalHref | dump | safe }} }
    ]
  }
  </script>

  <!-- Helpers -->
  <script>
    function toast(msg){var wrap=document.getElementById('toaster');if(!wrap) return;var el=document.createElement('div');el.className='alert alert-info shadow';el.innerHTML='<span>'+msg+'</span>';wrap.appendChild(el);setTimeout(function(){el.style.opacity='0';},2200);setTimeout(function(){el.remove();},2600);}
    function copyToClipboard(text){
      try{navigator.clipboard.writeText(text).then(function(){toast('Copied: '+text);});}
      catch(e){var t=document.createElement('textarea');t.value=text;document.body.appendChild(t);t.select();try{document.execCommand('copy');toast('Copied: '+text);}catch(_){}t.remove();}
    }
    (function(){
      var key='theme';
      var stored=localStorage.getItem(key);
      var radios=[].slice.call(document.querySelectorAll('.theme-controller'));
      function apply(name){
        document.documentElement.setAttribute('data-theme', name);
        localStorage.setItem(key, name);
        radios.forEach(function(r){ if(r.value===name) r.checked=true; });
        var meta=document.querySelector('meta[name="color-scheme"]');
        if(meta){ meta.content = (name==='nord') ? 'light dark' : 'dark light'; }
      }
      if(stored){ apply(stored); } else {
        var prefersDark=window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches;
        apply(prefersDark?'sunset':'nord');
      }
      document.addEventListener('change', function(e){
        var t=e.target; if(t && t.classList && t.classList.contains('theme-controller')){ apply(t.value); toast('Theme: '+t.value); }
      });
    })();
  </script>
</body>
</html>
