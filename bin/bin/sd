#!/usr/bin/env bash
set -Eeuo pipefail
. "$(cd "$(dirname "$0")" && pwd)/_lib.sh"
retry_exec "$@" --   "$REPO_ROOT/node_modules/.bin/sd"   "$TOOLS_DIR/sd/sd"   sd
echo "❌ sd not found." >&2
exit 127
