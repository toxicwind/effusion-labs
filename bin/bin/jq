#!/usr/bin/env bash
set -Eeuo pipefail
. "$(cd "$(dirname "$0")" && pwd)/_lib.sh"
retry_exec "$@" --   "$REPO_ROOT/node_modules/.bin/jq"   "$TOOLS_DIR/jq/jq"   jq
echo "❌ jq not found." >&2
exit 127
