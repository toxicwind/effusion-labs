#!/usr/bin/env bash
set -Eeuo pipefail
. "$(cd "$(dirname "$0")" && pwd)/_lib.sh"
retry_exec "$@" --   "$REPO_ROOT/node_modules/.bin/fdplain"   "$TOOLS_DIR/fdplain/fdplain"   fdplain
echo "❌ fdplain not found." >&2
exit 127
