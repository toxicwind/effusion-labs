#!/usr/bin/env bash
set -Eeuo pipefail
. "$(cd "$(dirname "$0")" && pwd)/_lib.sh"
retry_exec "$@" --   "$REPO_ROOT/node_modules/.bin/bat"   "$TOOLS_DIR/bat/bat"   bat
echo "❌ bat not found." >&2
exit 127
